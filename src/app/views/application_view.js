import Backbone from 'backbone';
import _ from 'underscore';
import $ from 'jquery';

import Application from 'app/models/pplication';

const ApplicationView = Backbone.view.extend({


})

export default ApplicationView
